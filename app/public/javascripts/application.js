document.observe('dom:loaded', function () { 
                                    elem = $('contact');
                                    var goof = ('kels' + 'eying' + 'rid@' + 'gmail.c' +'om');				
                                    elem.innerHTML = '<a href="mailto:' + goof + '">' + goof + '</a>';
					});	
			